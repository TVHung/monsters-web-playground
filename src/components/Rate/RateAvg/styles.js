import { makeStyles } from "@material-ui/core/styles";

export default makeStyles((theme) => ({
    score: {
        paddingTop: 10,
    },

    avg: {
        fontSize: 45,
        fontWeight: "bold",
    },

    sum: {
        fontSize: 30,
        color: "#7b7b7b",
    },

    textCount: {
        fontSize: 15,
        color: "#7b7b7b",
    },
}));
