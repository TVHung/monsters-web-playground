import { makeStyles } from "@material-ui/core/styles";

export default makeStyles((theme) => ({
    root: {
        width: "100%",
    },

    ul: {
        margin: 20,
        padding: 10,
    },

    li: {
        border: "1px solid grey",
        borderRadius: 10,
        paddingLeft: 20,
        marginBottom: 5,
    },
}));
